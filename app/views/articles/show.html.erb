<div>
  <h1><%= @article.title %></h1>
</div>

<div>
  <p>
    <%= simple_format(@article.body) %>
  </p>
</div>

<%= link_to "All articles", articles_path %> |
<%= link_to "Edit article", edit_article_path(@article.id) %> |
<%= link_to "Delete article", article_path(@article.id), method: :delete, data: {confirm: "Really delete?"} %>
